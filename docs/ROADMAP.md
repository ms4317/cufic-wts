# 로드맵

지금 어디까지 왔고 뭐가 남았는지. 상태가 바뀌면 갱신한다.

**현재 위치: §1·§2 완료 (DB 스키마 + 주문서/힌트 RPC).** 다음은 §3 학생 화면 연동 — UI 명세 대기 중.

> ⚠ **게임 규칙이 바뀌었다.** 즉시 체결 → 주문서 일괄 체결, 뉴스 → 등급제 힌트(조별 차등 지급).
> 프론트 UI는 아직 옛 모델(즉시 체결·전체 공개 뉴스) 그대로다. DB와 UI가 어긋나 있는 상태.

---

## 완료

### 학생 화면 (프론트엔드 단독)

- **라운드 루프** — R1(2022)~R3(2024). 연도별 시세에서 현재가·등락 파생, 라운드 진행 시
  12종목 재평가 → 손익 발생. 라운드 전환 요약 모달(내 보유종목 기준 최고/최저, 직전 대비 변화)
- **주문** — 시장가 매수/매도, 공매도 차단, 거래정지(가격 0) 차단, 평단 재계산, 실현손익,
  확인 모달(체결 후 예수금 표시), 수량 키보드 입력 + 비율 버튼(10/25/50/최대·전량)
- **계좌** — 평가금액·총손익 파생 계산, MY 모달 4탭(보유종목·체결내역·수익률·순위)
- **차트** — 종목별 결정론적 캔들, 실제 등락 방향 반영, 5일 이동평균, 그리기(펜·추세선·지우개, 종목별 보관)
- **재무제표** — 12종목×4년×5지표, 라운드 초과 연도 차단(스포일러), 탭해서 여는 설명, 종목 한 줄 소개
- **시황 뉴스** — 라운드 필터, 호재/악재↔등락 정합, 전문 모달, 관련 종목 → 종목 이동, 도착 토스트(탭 이동)
- **조별 순위** — 헤더 배지 + 하단 상시 현황판 + MY 탭. **다른 조는 더미** (`예시` 배지로 표시)
- **테마** — 다크/라이트, CSS 변수 전면, FOUC 방지
- **태블릿** — 1024~1279 축소, 세로 회전 안내, 터치 44px, hover 전용 없음
- **접근성** — 모달 포커스 트랩 + 닫을 때 복귀

### 품질 장치

- **Vitest 89개** — 계좌 파생, 주문 한도, 데이터 정합성(뉴스↔주가 전수 대조), 과거 버그 4건 회귀
- **actions.js** — 상태 조작을 async `{ok, error}` 계층으로 분리. Supabase 교체 지점
- **git** — 첫 커밋 완료. `.env` 커밋 차단

---

### 백엔드 (Supabase, 프로젝트 `cufic_wts`)

- **§1 스키마** — `game_state` / `stocks` / `teams` / `positions` / `trades` /
  `round_snapshots` / `order_sheets` / `hints` / `hint_grants`. 마이그레이션 8개 적용.
  RLS: 읽기만 선별 허용, 쓰기 정책 없음(= RPC로만)
- **§2 RPC** — `save_order_sheet` / `advance_round`(일괄 체결) / `reset_game` /
  `grant_hint` / `get_my_hints` / `get_my_order_sheet` / `login_team` / `team_equity`.
  실제 호출 검증 62건 통과
- **관리자 보호** — 관리자 RPC 전체가 `p_admin_secret` 필수. 비밀은 `private` 스키마
  (REST 경로 없음), 미설정 시 fail closed. 검증 26건 통과
- **`seed.sql`은 `data.js`에서 생성** (`node scripts/gen-seed.mjs`) — 정합성 테스트가
  검사하는 데이터와 DB의 원천이 하나

## 진행 중

### §3 학생 화면 연동 — **UI 명세 대기**

게임 규칙이 바뀌어 프론트 UI 개편이 선행돼야 한다. 현재 UI는 즉시 체결·전체 공개 뉴스 모델이다.

| | 내용 | 핵심 체크포인트 |
|---|---|---|
| §3 | actions.js 내부를 Supabase 호출로 교체 | 재접속 복원, 오프라인 실패 토스트 |
| §4 | 관리자 화면 4탭 (진행·조 관리·종목/가격·리더보드) | 조 수·시드 하드코딩 없음 |
| §5 | Realtime 구독 | 브라우저 2개 + 관리자 시나리오 |
| §6 | 마감 | 기존 89개 유지, README |

---

## 대기 (일정 미정)

- **가상 종목 데이터 이식** — 지금 데이터는 실제 종목명 + **검증되지 않은** 주가·재무.
  **실제 수업 전 반드시 교체.** 종목명 방식(창작/패러디/기호) 미정. 시드 스크립트 교체로 처리
- **실기기 테스트** — 실제 태블릿에서 그리기·터치 영역·회전 확인. 지금은 헤드리스 브라우저 검증만
- **교육장 와이파이 확인** — 클라우드 Supabase 접속 가능 여부. 동시 접속 규모(조 개수) 미정
- **배포** — Vercel 등 미정
- **순위 상시 노출 유지 여부** — 팀 결정 대기. 일단 유지
- **봉 주기 실데이터화** — 일/주/월/년이 전부 같은 구간. 알려진 껍데기
- **파일 정리** — `index.css` 1,400줄 분리, `data.js` 콘텐츠 분리. 서버 연동 뒤로

## 보류

레버리지 ETF(기초자산 대기), 텔레그램 오류 보고(후순위).

---

## 알려진 위험

1. ~~**관리자 RPC가 anon에게 열려 있다**~~ → **2026-07-17 해소.** `p_admin_secret` 필수화,
   비밀은 `private` 스키마. 검증 26건 통과. 남은 한계: 비밀이 관리자 브라우저 번들에 들어간다
2. **검증되지 않은 데이터** — 학생이 지어낸 재무 수치를 사실로 배운다. 가상 종목 이식 전까지 데모 전용
3. **프론트 UI가 옛 모델** — 즉시 체결·전체 공개 뉴스. DB와 어긋나 있다
4. **개발 DB가 곧 대회 DB** — 대회 전 `reset_game` 초기화 리허설 필수. 테스트 조(`TEST-01`,
   `TIGER-03`)도 지워야 한다
5. **힌트 Realtime 불가** — RLS 대신 RPC로 격리해서 실시간 구독이 안 된다. §5에서 폴링 등으로 대응
